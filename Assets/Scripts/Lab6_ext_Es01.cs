using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lab6_ext_Es01 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float Pi = 3.14159f ;
        string a = Pi.ToString("F2");
        Debug.Log( a );
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
